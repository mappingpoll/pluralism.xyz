{
  "scripts": {
    "lint": "yarn run eslint --ext 'js,jsx' .",
    "lint:fix": "yarn run eslint --fix --ext 'js,jsx' .",
    "serve:dev": "http-server public/",
    "build:dev": "NODE_ENV=development yarn node build.js",
    "build:prod": "NODE_ENV=production yarn node build.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "esbuild": "^0.14.2"
  },
  "devDependencies": {
    "eslint": "^8.6.0",
    "eslint-config-preact": "^1.3.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-prettier": "^0.0.0-empty",
    "jest": "^27.4.7",
    "prettier": "^2.5.1"
  }
}
